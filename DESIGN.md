# 本地候选人工作台

独立于营销官网，面向长时间阅读和筛选。采用档案阅览台：浅暖灰工作区、白色简历纸面、深墨绿操作色、细分隔线。桌面左侧候选人索引，右侧详情；窄屏顺序堆叠。系统中文字体用于密集信息阅读，标题保持克制。真实空状态，不填造候选人；选择候选人立即显示简介，完整简历显式加载。错误保留原因且不替代成空列表。

用户要求直接实现，本轮采用代码实现，不保存视觉制作的永久偏好。

## Layout

本地候选人页的飞书同步工具栏沿用现有操作行：桌面横排并可换行，680px 及以下改为左对齐纵向排列；同步状态独占一行，长错误文本自然换行。

## Components

飞书同步复用浅灰绿次级按钮，搭配低强调说明文字和深墨绿表格链接，保持克制的操作界面。进度、完成和失败信息在工具栏内通过 `aria-live="polite"` 持续呈现，失败使用现有红褐色提示并保留具体原因。共享操作忙碌时禁用同步按钮，同步中按钮显示“正在同步到飞书…”。

关键词搜索筛选使用原生 `dialog`，延续白纸面、细分隔线和墨绿选中态。Boss 当前页面动态提供学历要求、院校要求、经验要求、牛人活跃度、跳槽频率五类选项，以 `fieldset` / `legend` 分组；院校用复选框，其余用单选框。说明文字明确城市、岗位、薪资等其他条件沿用 Boss 当前设置，学历和经验暂支持预设范围。选项在桌面和移动端自然换行；窄屏缩小内边距，底部操作允许换行，对话框内容可滚动。主按钮“应用并搜索”提交所选条件，次级按钮“本面板设为不限”仅重置面板选项，“取消”关闭对话框；加载失败在面板内显示具体错误。
