# Product
<!-- impeccable:product-schema 1 -->

## Platform
web

## Users
在本机筛选 Boss 直聘候选人的招聘人员。

## Product Purpose
通过可视化工作台浏览真实候选人简介和在线简历，底层调用现有 CLI。

## Operating Context
本机 Node.js 和 Chrome，人工完成 Boss 登录。用户已授权直接实现本机网页版。

## Capabilities and Constraints
推荐、关键词搜索、简介和简历图片。明确关闭 OCR，不发送截图到 OCR 服务。只展示平台当前可见数据，简历预览受平台额度限制。保留 CLI 文本命令行为；结构化输出显式选择。官网保持独立。

## Product Principles
- 数据真实，失败直接显示。
- 候选人身份和来源必须明确。
- 先完成查看流程，不添加消息发送功能。

## 本地采集工作流
在线列表及原始返回字段默认写入 SQLite；简历图片单独持久保存并关联候选人。独立的本地候选人页提供查询、分页和离线阅读。一键批量操作读取一次当前列表，按目标数量串行打开简历，不足时滚动续采，跳过本地已有简历，每次间隔 15–25 秒，支持停止，失败立即结束。不会发送消息。浏览器不主动抢焦点。
